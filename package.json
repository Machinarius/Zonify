{
  "name": "apprevention",
  "version": "0.0.0",
  "author": "Yunerkis Leal",
  "private": true,
  "scripts": {
    "docker": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "dev": "lerna run dev --stream",
    "dev:api": "lerna --scope @apprevention/api run dev --stream",
    "dev:client": "lerna --scope @apprevention/client run dev --stream"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^4.0.0",
    "typescript": "^4.2.4"
  },
  "dependencies": { }
}
